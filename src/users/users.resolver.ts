import { Query } from '@nestjs/common';
import { Args, Resolver } from '@nestjs/graphql';

@Resolver()
export class UsersResolver {
  // @Query()
  // async getUser(
  //   @Args('data') data:
  // )
}
